
MEMORY {
    BIN:      type = ro, file = %O, define = yes,  start = $A000, size = $2000, fill = yes;
}
SEGMENTS {
    HEADER:         load = BIN,       type = ro;
    DOCS:           load = BIN,       type = ro;
}
